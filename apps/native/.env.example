# LAN IP of the dev machine, not localhost — required for physical devices /
# Expo Go. Works for web preview and iOS Simulator too. Android emulator:
# use http://10.0.2.2:3000 instead.
EXPO_PUBLIC_SERVER_URL=http://192.168.1.10:3000
